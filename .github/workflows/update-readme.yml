name: 🔄 Dynamic README Updater

on:
  schedule:
    - cron: "0 1 * * *"   # daily at 01:00 UTC (06:30 IST)
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"         # avoid recursive trigger from bot commits

jobs:
 # Disabled since all dynamic sections (Recent Activity, Playground) were removed.
